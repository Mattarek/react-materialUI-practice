import React from 'react';
import Grid from '@mui/material/Grid';

export const Authentication = () => {
    return (
        <Grid
            item
            xs={8}>
            Authentication
        </Grid>
    );
};
